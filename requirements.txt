# What the review console needs to run. Install with:
#   pip install -r requirements.txt
# Model export and retraining extras live in requirements-train.txt.
opencv-python>=4.10.0
numpy>=1.26.0
PySide6>=6.11.2
mss>=9.0.0
onnxruntime>=1.20.0
